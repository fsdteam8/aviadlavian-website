import ResetPasswordForm from "@/features/auth/reset-password/components/reset-password-form";

const page = () => {
  return (
    <div>
      <ResetPasswordForm />
    </div>
  );
};

export default page;
